Added — `AGENTS.md` now has a "Reader4 pipeline" section capturing all four nuances: export ≠ archive (and why), the looks-stalled-but-isn't button, manual-move drift and how to reconcile it, and the known failures. Future sessions (and future you) won't have to rediscover any of it. The auto-commit will pick it up... actually no — `AGENTS.md` is gitignored per your choice, so it stays local-only, but it's the file every Claude session reads first, which is what matters.

**What's next, split by who does it:**

**You:**
1. **Work through the 45 Inbox notes with `/reader4-review`** — at your own pace, a handful per sitting. Each confirmation files a note *and* clears its Shortlist twin. This is now your steady-state reading workflow, not a project task.
2. **Check the two keys in `~/Downloads/reader4/.env` are rotated** (the Readwise token + the stray OpenAI key) — you rotated the vault ones; just confirm these were included.

**Me (in rough order, say go on any):**
1. **Build the status digest** — the "where was I up to?" note. Now genuinely useful: it'll tell you how many Inbox notes await, which are highest quality (file those first), what's been waiting longest, plus your Readwise queue size. Quick job.
2. **Backlog amnesty session** — the bigger one: we write down what you actually want to read, I score your Readwise `Later` pile against it, you approve deleting the bottom half, and we set the 5–7 item Shortlist cap. Needs you present for the preferences conversation (grill-dom style), so book it for when you have half an hour.
3. **Note-style spec** — I study your best notes and write down your formatting style so AI output can match it. Can happen any time, no input needed from you until the pick-your-preferred-examples step.

And parked but not forgotten (all in the tracking doc): the 3 failed exports investigation, the arXiv-to-Reader intake, and the fun downstream stuff (chat-with-your-vault, resurfacing, flashcard drafts).

My suggestion: I build the digest now while you chip away at the Inbox queue. Green light?